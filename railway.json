{
  "build": {
    "command": "ls -l && echo --- && ls -l /app && echo --- && cat main.py | head -n 5",
    "env": {
      "PYTHONUNBUFFERED": "1"
    }
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}